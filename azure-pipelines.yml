#//TESTING COMMENT HERE SOMETHING SOMETHING one more test
trigger:
- main
schedules:
    - cron: '* * * * *'
      always: true
      branches:
          include:
              - main
pool: default
steps:
- script: echo Hello from relay test
